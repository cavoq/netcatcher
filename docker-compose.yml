version: '3.8'


services:
  netcatcher:
    build:
      context: .
      dockerfile: Dockerfile
    network_mode: host
    image: netcatcher:0.1.3
    command: ["/bin/bash", "/netcatcher/netcatcher.sh"]
    env_file:
      - .env
